Put the native PJSIP command-line engine here:

Windows:
  tools/pjsua/pjsua.exe

This project controls pjsua.exe from Electron to provide classic SIP UDP/TCP/TLS registration and calls.
You can build pjsua from PJSIP source or copy a trusted internal build.
